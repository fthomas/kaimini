Input:
 - BLOCK Kaimini???:
   - add entries to select the driver and its options

Output:
 - BLOCK KaiminiInfo:
   - add number of function calls
   - add information about the used driver

GSLDriver:
 - runSimplex:
    - determine the parameter errors
    - call GenericFit::processMinimum()
 - runSimulatedAnnealing:
    - make it work!

SimpleSLHAFit:
 - selectCalculator:
    - sanitize user input (http://xkcd.com/327/)
